<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="../input.css">
    <title>Course Detail</title>
</head>

<body>
    <%-include ('partials/_loggedNavbar') %>
        <div class="errors">
            <% if (error) { %>
                <%-include ('partials/_errors') %>
                    <% } %>
        </div>
        <h1>
            <%= course.name %>
        </h1>
        <a href="/courses/<%= course.id %>/enroll/">
            <button>Enroll Now!</button>
        </a>
        <br>
        <div class="profilePicture">
            <img src="<%= teacher.UserDetail.profilePicture %>" alt="teacherPicture">
        </div>
        <br>
        <h2>
            <%= teacher.UserDetail.fullName %>
        </h2>
        <h4>
            <%= course.level %>
        </h4>
        <h5>
            <%= hourFormatter(course.duration) %>
        </h5>
        <p>
            <%= course.description %>
        </p>
        <a href="/courses/<%= course.id %>/delete">
            <button>Delete Course</button>
        </a>
</body>

</html>